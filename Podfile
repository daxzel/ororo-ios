# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ororo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'RealmSwift', '~> 2.8'
  pod 'Alamofire', '~> 4.5'
  pod 'SwiftyJSON', '~> 3.1'
  pod 'DropDown', '~> 2'
  pod 'DAPagesContainer', '~> 1.0'

  # Pods for ororo

  target 'ororoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ororoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
